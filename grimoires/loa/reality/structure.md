# Structure — Extracted Reality

> Generated by `/ride` (riding-codebase). CODE IS TRUTH. Source: `git ls-files` + direct reads, 2026-06-20.

## Application layout (tracked, excluding `.gitkeep`)

```
TurnTrace/
├── agents/runtime/        2 files — runtime agents (the "fast and dumb" side; NFR-1)
│   ├── random_legal.py        floor baseline: uniform-random legal selection
│   └── scripted_baseline.py   frozen deterministic baseline: lowest-index-first
├── sim/                   5 files — the SINGLE blast radius (only zone that touches cabt)
│   ├── adapter.py             SimAdapter: wraps cg.game; Competition-Data-free views
│   ├── _env.py                path/config/deck resolution (no cabt import)
│   ├── probe.py               capability probe (first Sprint-00 executable)
│   ├── capabilities.json      machine-readable probe output (consumed by adapter)
│   └── README.md              generated capability findings (by probe.py)
├── eval/                  7 files — match/eval drivers + validators
│   ├── run_match.py           one match → match-summary.json + decision-trace.jsonl
│   ├── run_eval.py            N matches → one sealed immutable run dir
│   ├── canonical_json.py      deterministic JSON + SHA-256 (trace hashing)
│   ├── validate.py            flat-file schema validator (run-dir gate)
│   ├── hygiene_check.py       pre-commit Competition-Data path guard
│   ├── mirror_validate.py     candidate-vs-self pre-submission smoke
│   └── schemas.md             schema design authority (field lists)
├── analysis/              7 files — offline, read-only over sealed run dirs (NFR-1)
│   ├── aggregate.py           run dir → summary.csv + ledger row
│   ├── delta_report.py        same-regime A-vs-B per-metric delta
│   ├── dispersion_report.py   cross-run descriptive dispersion (K runs, 7 stats)
│   ├── evidence_summary.py    sanitized K-batch evidence summary + validator
│   ├── failure_report.py      coarse failure-mode counts
│   ├── replay_check.py        reproducibility (audit-trail + byte-identical tiers)
│   └── e2e_validate.py        PRD G-1..G-7 goal validation against artifacts
├── frozen/               10 files — immutable evaluation inputs (regimes/seeds/...)
│   ├── regimes/               regime-v001, v002, v003 (4-tuple test definitions)
│   ├── seeds/                 seed-set-v001..v003 (fixed match-index lists)
│   ├── opponents/             opponent-pool-v001
│   ├── decks/                 deck-pool-v001 (hashes + refs only, no card data)
│   └── metrics/               metrics-spec-v001 (correctness gates, not strength)
├── tests/                 3 files — stdlib unittest + plain-assert smokes
│   ├── test_smokes.py             50+ methods / 15 TestCase classes (needs cabt)
│   ├── test_evidence_summary.py   ~14 standalone checks (no cabt)
│   └── test_import_direction.py   AST zone-import-direction lint (no cabt)
├── config/eval_config.json   cg_dir / deck_file / sim_version pins
├── docs/                 63 files — durable cycle docs (cycle-000..007), ledger,
│   │                       claim-ceiling, loop-contract, operator decisions
│   ├── ledger.md              append-only experiment ledger (3 data rows)
│   ├── claim-ceiling.md       standing ceiling: Rung 2 ("beats random-legal")
│   └── cycles/cycle-00N/      per-cycle PRD/SDD/sprint-plan/reports/closeout
├── runs/                  0 tracked files (.gitkeep only) — run outputs are local/git-ignored
├── vendor/                0 tracked files (.gitkeep only)
├── README.md             repo-level readme (STALE — see drift-report.md)
└── PROCESS.md            workflow documentation
```

## Zone import direction (enforced by tests/test_import_direction.py)

| Zone | May import | Touches cabt? |
|------|-----------|---------------|
| `agents/runtime/` | stdlib only | No |
| `sim/` | stdlib + cabt | **Yes (only here)** |
| `eval/` | stdlib + sim + agents/runtime + analysis | via `sim` |
| `analysis/` | stdlib + analysis | No |

## Counts

- Tracked files: 139 total; 21 Python files; ~5,689 Python LOC.
- App Python: `agents` 2, `sim` 3, `eval` 6, `analysis` 7, `tests` 3.
- Third-party deps in app code: **none** (stdlib-only verified; NFR-7). `loa_cheval`/`pytest`/`trace_analyzer` imports exist ONLY under `.claude/` framework adapters, not in TurnTrace app code.
