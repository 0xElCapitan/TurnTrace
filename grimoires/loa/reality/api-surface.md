# API Surface — Extracted Reality

> Public functions/classes per module, with `file:line`. Generated by `/ride`. Signatures verbatim from source.

## agents/runtime/

`random_legal.py` — `AGENT_ID="random_legal"`, `AGENT_VERSION="random_legal-v001"` (`:23-24`)
- `select(n_options, min_count, max_count, rng=None) -> list[int]` (`:27`) — uniform-random `max_count` distinct indices.
- `agent(obs_dict) -> list[int]` (`:42`) — Kaggle/cabt submission contract; raises if `select is None`.

`scripted_baseline.py` — `AGENT_ID="scripted_baseline"`, `AGENT_VERSION="scripted-v001"` (`:33-34`)
- `select(n_options, min_count, max_count, rng=None) -> list[int]` (`:37`) — deterministic `[0..k-1]` (no RNG).
- `agent(obs_dict) -> list[int]` (`:53`) — submission contract.

## sim/

`adapter.py` — `class SimAdapter` (`:57`)
- `__init__(cg_dir=None, cfg=None)` (`:65`) — imports `cg.game` (loads cg.dll/libcg.so).
- `start_match(deck0, deck1) -> dict` (`:80`); `step(selection) -> dict` (`:92`); `finish() -> None` (`:100`).
- `capabilities() -> dict` (`:109`) — flags + measured throughput from `capabilities.json` or conservative defaults.
- Static views (Competition-Data-free): `terminal` (`:137`), `your_index` (`:152`), `turn` (`:157`), `select_context` (`:162`), `is_deck_selection` (`:167`), `legal_options` (`:171`), `legal_digest` (`:185`), `public_summary` (`:193`), `private_summary` (`:219`), `selected_action_view` (`:241`), `outcome_for_player` (`:252`), `ending_cause` (`:261`).

`_env.py` — `load_config(path=None) -> dict` (`:23`); `resolve_cg_dir(cfg=None) -> Path` (`:37`); `resolve_deck_file(cfg=None) -> Path` (`:48`); `read_deck(deck_file) -> list[int]` (`:59`, first 60 ids).

`probe.py` — CLI. `run_one_match(game, deck, rng, capture=False) -> dict` (`:114`); `probe_invalid_action_detectable(game, deck) -> bool|None` (`:175`); `build_caps(cfg) -> dict` (`:204`); `render_readme(caps, deck_present) -> str` (`:253`); `main() -> int` (`:351`).

## eval/

`canonical_json.py` — `canonical_dumps(obj) -> str`; `sha256_hex(data) -> str`; `hash_canonical(obj) -> str`. Stdlib only, no CLI.

`run_match.py` — `AGENTS` registry (`:53`, keys `random_legal`/`scripted_baseline`); `deck_hash(deck) -> str` (`:63`); `play_match(adapter, agents, decks, inputs, caps, rng) -> dict` (`:75`, returns `{summary, trace}`); `write_match(out_dir, match_id, result_obj) -> tuple[Path,Path]` (`:222`); `recompute_trace_hash(trace_path) -> str` (`:240`); `run_single(inputs, out_dir, cfg, deck_a, deck_b, rng) -> dict` (`:252`); `main(argv=None) -> int` (`:261`). `REQUIRED_INPUTS` 10-tuple (`:44`). FM-01 guard: an error is `result="error"`, never a loss (`:162-170`).

`run_eval.py` — `run_eval(run_id, out_dir, regime_id="regime-v001", ledger_path=None, agent_id="random_legal", write_ledger=False, ledger_notes=None) -> dict` (`:95`); exceptions `ImmutabilityRefusal` (`:299`), `AgentInitError` (`:303`); `main(argv=None) -> int` (`:307`). Immutability guard before any write (`:138-154`); deck-drift guard vs frozen hash (`:128-134`).

`validate.py` — `validate_match_summary(rec, label) -> list[str]`; `validate_trace(rows, label) -> list[str]`; `validate_trace_self_consistency(summary, rows, label) -> list[str]`; `validate_manifest(man) -> list[str]`; `validate_hashes(txt) -> list[str]`; `validate_run_dir(run_dir) -> list[str]`; `main(argv=None) -> int`.

`hygiene_check.py` — `find_violations(paths) -> list[tuple[str,str]]`; `staged_paths() -> list[str]`; `main(argv=None) -> int`.

`mirror_validate.py` — `play_mirror_match(adapter, candidate, deck) -> dict`; `main(argv=None) -> int`. Imports `SimAdapter`, `run_match.AGENTS`, `sim/_env`.

## analysis/

`aggregate.py` — `aggregate_run(run_dir) -> dict` (`:56`); `write_summary_csv(run_dir, stats) -> Path` (`:92`); `append_ledger_row(ledger_path, fields) -> bool` (`:125`); `aggregate_and_ledger(run_dir, ledger_path, ...) -> dict` (`:152`). Self-contained.

`delta_report.py` — `compare(run_a, run_b) -> dict` (`:132`, raises `CrossRegimeRefusal`); `render(rep) -> str` (`:178`). Imports `aggregate`.

`dispersion_report.py` — `descriptive_stats(values) -> dict` (`:94`); `disperse(run_dirs) -> dict` (`:117`, raises `MixedRegimeRefusal`); `render` (`:197`); `render_json` (`:243`). Imports `aggregate`. Descriptive stats only (no variance/std/inferential).

`evidence_summary.py` — `build_summary(run_dirs) -> dict` (`:137`); `validate_summary(obj) -> list[tuple[str,str]]` (`:394`); `render_json(summary) -> str` (`:219`). Imports `aggregate`, `dispersion_report`. Generate / `--validate` / `--promotion-check` modes.

`failure_report.py` — `aggregate_failures(run_dir) -> dict` (`:78`); `render` (`:125`); `render_json` (`:165`). Self-contained.

`replay_check.py` — `audit_trail_equality(run_dir) -> dict` (`:69`); `byte_identical(run_dir, replay_run) -> dict` (`:98`, DEAD PATH while unseeded — `:101`); `replay_check(run_dir, replay_run=None) -> dict` (`:121`); `render` (`:149`). Carries a parity-tested stdlib copy of canonical hashing.

`e2e_validate.py` — `validate(run_a, run_b) -> dict` (`:151`); `render(rep) -> str` (`:162`); 7 per-goal checkers `g1_run_matches`..`g7_report` (`:54-138`). Imports `aggregate`, `delta_report`, `replay_check`.
