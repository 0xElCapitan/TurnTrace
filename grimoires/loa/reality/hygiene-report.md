# Code Hygiene Report

> Generated by `/ride` 2026-06-20. Items flagged for HUMAN DECISION — `/ride` flags,
> it does not fix. CODE IS TRUTH.

## Summary

The app codebase is unusually clean: stdlib-only, zone-separated, every module
docstring-documented and acceptance-criteria-tagged. No TODO/FIXME/HACK/XXX markers
exist in TurnTrace application code. Very little to flag.

## Flagged items

### H-1 [INFO] Deliberate dead path, correctly marked
- `analysis/replay_check.py:101` — `byte_identical()` is marked `# loa:shortcut: DEAD PATH —
  unreachable while seed_controlled=false`. This is **intentional and well-documented**:
  the function exists for the future seeded regime but cannot run today. It is tested
  (ReplayCheckSmoke confirms it still skips rather than fabricating a seed).
- **Decision:** Keep. The marker names the ceiling (seed_controlled=false) and the
  function is the honest forward-compat stub. No action needed.

### H-2 [INFO] Three `loa:shortcut:` markers — all have ceilings, all benign
- `analysis/dispersion_report.py:39` — reuses `aggregate.aggregate_run()` (intra-zone). Fine.
- `analysis/evidence_summary.py:33` — hygiene rules are a parity-tested stdlib local copy
  of `eval/hygiene_check.py` (avoids cross-zone import). Parity is test-enforced.
- `analysis/replay_check.py:101` — see H-1.
- **Decision:** All three are legitimate simplifications with named ceilings. None is a
  silent rot. No action needed.

### H-3 [INFO] Duplicated canonical-hash logic (deliberate)
- `analysis/replay_check.py` carries its own stdlib copy of the canonical-JSON hash rather
  than importing `eval/canonical_json.py`, to preserve the analysis→analysis-only import
  rule (NFR-1 zone separation). Parity is asserted in tests (ReplayCheckSmoke canonical
  parity check).
- **Decision:** Keep — the duplication is a conscious trade of DRY for zone purity, and
  the test net catches divergence. Flagged only so a future reader does not "fix" it.

## Not flagged (verified clean)

- **No files outside standard directories** — every tracked path sits under a declared zone.
- **No commented-out code blocks** of significance in app modules.
- **No dependency conflicts** — there are no third-party dependencies in app code.
- **No temp/WIP folders** in tracked files (`vendor/`, `runs/` hold only `.gitkeep`).
- **No secrets** in app code (env-var indirection only; `TURNTRACE_CG_DIR`, `TURNTRACE_DECK_FILE`).

## Note on `.claude/`

`grep` for TODO/XXX and third-party imports surfaced hits under `.claude/adapters/`
(loa_cheval, pytest, trace_analyzer). These are **framework System-Zone files**, not
TurnTrace application code, and are out of scope for this ride. They are not a hygiene
concern for the project.
