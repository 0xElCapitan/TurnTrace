# Three-Way Drift Report — README vs Code Reality

> Generated by `/ride` (riding-codebase) 2026-06-20. CODE IS TRUTH.
> Scope: re-grounding the repo-level `README.md` against extracted code reality
> (`grimoires/loa/reality/*`). Drift policy: `code` (update docs to match code).

## Summary

| Metric | Value |
|--------|-------|
| Primary document audited | `README.md` (63 lines) |
| README last modified | **2026-06-17 21:50** — the bootstrap commit (`d894355`), never touched since |
| Code last modified | 2026-06-20 (3 cycles of work landed *after* the README) |
| Critical drift items (Ghost / Hallucinated) | **3** |
| Stale items | **2** |
| Aligned items | **8** (the directory-layout section is accurate) |
| **Drift score** | **5.2 / 10** — structurally accurate, but the headline status is false |

The README's *directory map* is correct and useful. Its *status narrative* ("Bootstrap
only / no simulator integration / no agent implementation") is flatly contradicted by
the code and is the single highest-severity finding. This is a clean, real drift result
— not a "too small to surface drift" case.

## Drift score derivation

Headline status claims are weighted heaviest. 3 critical (status falsehoods) + 2 stale,
against 8 aligned structural claims. The structural accuracy keeps the score off the
floor; the false headline keeps it well below passing. Score `5.2/10`.

---

## CRITICAL — Hallucinated / Ghost claims (code contradicts the doc)

### D-1 [GHOST → HALLUCINATED] "No simulator integration exists yet."
- **README:** lines 11–13: *"No simulator integration exists yet. No agent implementation exists yet. No gameplay-strength claims are made."*
- **Reality:** A full cabt integration exists and has been **run**. `sim/adapter.py:73` imports `cg.game`; `sim/probe.py` executed a live probe on 2026-06-18 confirming matches run at ~92/sec (`sim/capabilities.json`, `sim/README.md:11-33`). Two agents are implemented: `agents/runtime/random_legal.py`, `agents/runtime/scripted_baseline.py`. A complete match/eval pipeline exists (`eval/run_match.py`, `eval/run_eval.py`) plus 7 analysis modules.
- **Severity:** CRITICAL. The README's central status claim is false.
- **Evidence:** `sim/adapter.py:73`, `sim/probe.py:351-451`, `sim/capabilities.json:11-15`, `eval/run_eval.py:95`, `agents/runtime/*.py`.

### D-2 [HALLUCINATED] "No gameplay-strength claims are made" / "Claim ceiling: repository shell only."
- **README:** lines 13, 58–62: claims TurnTrace *"only claims to be a repository shell for a future data-loop harness"* and makes no relative-strength claim.
- **Reality:** The standing claim ceiling is **Rung 2 — "beats random-legal"**, earned Cycle-007 (`docs/claim-ceiling.md:1-104`). The ledger records a **Rung-2 PASS**: candidate `scripted-v001` beats baseline `random_legal-v001` under regime-v003 via a descriptive disjoint-bands rule over K=20 batches at n=500 (`docs/ledger.md` row `run-v003-c-1..20`; `docs/cycles/cycle-007/07-closeout.md`). This is a *bounded, relative, local* strength claim — exactly what the README says does not exist.
- **Severity:** CRITICAL. Note: the README is *conservative* here (under-claims), which is safer than over-claiming, but it is still false and hides the project's actual posture.
- **Evidence:** `docs/claim-ceiling.md:1-104`, `docs/ledger.md`, `docs/cycles/cycle-007/07-closeout.md`.

### D-3 [HALLUCINATED] "Current status: Bootstrap only."
- **README:** lines 7–9.
- **Reality:** The repo is through **7 development cycles** (`docs/cycles/cycle-000`…`cycle-007`, 63 tracked docs). Sprint 00 (evidence loop) and Sprint 02 (explanation loop) shipped (`git log`: `ffe16a8`, `9771436`). 21 Python files / ~5,689 LOC of working, tested code exist.
- **Severity:** CRITICAL.
- **Evidence:** `git log` (50 commits, first 2026-06-17, last 2026-06-20); `docs/cycles/`; `git ls-files '*.py'`.

---

## STALE — directionally right, details now wrong

### D-4 [STALE] "Planning and operator artifacts belong under `grimoires/loa/a2a/`."
- **README:** lines 50–52.
- **Reality:** `grimoires/loa/a2a/` exists but holds **0 tracked non-gitkeep files**; durable planning/operator artifacts actually live under `docs/` — `docs/cycles/cycle-NNN/` (PRDs, SDDs, sprint plans) and `docs/operator/` (loop contract, decision registers). The `a2a/` dir is used for ephemeral trajectory logs, not the durable planning artifacts the README points at.
- **Severity:** MEDIUM (misdirects a reader looking for planning docs).
- **Evidence:** `git ls-files grimoires/loa/a2a` (empty); `docs/cycles/*`, `docs/operator/*`.

### D-5 [STALE] Implied "nothing runs yet."
- **README:** the whole status section implies no executable surface.
- **Reality:** 12+ CLI entry points exist and run (`grimoires/loa/reality/entry-points.md`), gated by 3 test files (50+ smoke methods in `tests/test_smokes.py`).
- **Severity:** MEDIUM.
- **Evidence:** `tests/test_smokes.py`, `eval/*.py`, `analysis/*.py`.

---

## ALIGNED — README claims the code supports (keep these)

| # | README claim (lines 26–48) | Status | Evidence |
|---|----------------------------|--------|----------|
| A-1 | Runtime agent code under `agents/runtime/` | ALIGNED | `agents/runtime/{random_legal,scripted_baseline}.py` |
| A-2 | Simulator adapter under `sim/` | ALIGNED | `sim/adapter.py`, `sim/_env.py`, `sim/probe.py` |
| A-3 | Evaluation runners under `eval/` | ALIGNED | `eval/run_match.py`, `eval/run_eval.py` |
| A-4 | Offline analysis under `analysis/` | ALIGNED | 7 modules in `analysis/` |
| A-5 | Frozen evaluation inputs under `frozen/` | ALIGNED | `frozen/{regimes,seeds,opponents,decks,metrics}/` |
| A-6 | Generated run outputs under `runs/` | ALIGNED | `runs/` exists; outputs local/git-ignored (only `.gitkeep` tracked) — matches design |
| A-7 | Durable cycle docs under `docs/cycles/` | ALIGNED | `docs/cycles/cycle-000..007/` |
| A-8 | Core posture (data before agents; freeze baselines; immutable artifacts; claims tied to evidence) | ALIGNED | immutability guard `run_eval.py:138`; frozen inputs; ledger discipline |

The "Early architecture" section (lines 24–56) is the README's strongest part — every
path it names exists and is used as described, except the `a2a/` misdirection (D-4).

---

## Re-grounding recommendations (for Step 2 of the sprint)

1. **Replace the "Current status" + "Claim ceiling" sections** (README lines 7–13, 58–62)
   with the real posture: working evidence loop through Cycle-007, standing ceiling
   **Rung 2 — "beats random-legal"**, pointing at `docs/claim-ceiling.md` and `docs/ledger.md`
   as authorities. Preserve the *honesty* of the original (no inferential/absolute strength
   claims) while stating what HAS been earned.
2. **Keep** the directory-layout section nearly verbatim — it is accurate (A-1..A-8).
3. **Fix D-4:** point planning/operator readers at `docs/cycles/` and `docs/operator/`;
   describe `grimoires/loa/a2a/` as trajectory/ephemeral, not durable planning.
4. **Add** a one-line "How to run" pointer (probe → run_eval → analysis) and the
   stdlib-only + Competition-Data-local invariants — both are load-bearing and absent.
5. Carry forward the forbidden-agent-words discipline (strong/competitive/optimal/
   calibrated/complete) so the re-grounded README stays inside the ledger's ceiling.
