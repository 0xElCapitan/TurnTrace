# Consistency Report

> Generated by `/ride` 2026-06-20. Naming/pattern consistency across the codebase.
> `/ride` flags; it does not refactor. CODE IS TRUTH.

## Consistency score: 9 / 10

The codebase is highly consistent. Naming, module structure, error handling, and the
docstring convention are uniform across all 21 files. The one point off is for two minor,
already-deliberate divergences (noted below), not for sloppiness.

## Patterns observed (uniform)

| Dimension | Convention | Evidence |
|-----------|-----------|----------|
| File / module names | `snake_case.py` | all 21 files |
| Agent identity | `AGENT_ID` + `AGENT_VERSION` module constants | `random_legal.py:23`, `scripted_baseline.py:33` |
| Agent contract | dual `select(...)` (in-process) + `agent(obs_dict)` (submission) | both agents |
| CLI shape | stdlib `argparse`, `main(argv=None) -> int`, `raise SystemExit(main())` | run_match, run_eval, validate, mirror_validate, all analysis CLIs |
| Exit-code discipline | documented per module (0 ok, 1 input, 2 agent-init, 3 guard) | run_match.py:15, run_eval.py:34 |
| Error handling | typed exceptions per concern (`ImmutabilityRefusal`, `AgentInitError`, `CrossRegimeRefusal`, `MixedRegimeRefusal`) | run_eval.py:299, delta_report.py, dispersion_report.py |
| Refusal idiom | "REFUSED — <reason>" with a dedicated nonzero exit | run_eval.py, delta_report.py, dispersion_report.py |
| Time | `datetime.now(timezone.utc).isoformat()` via `_now()` | run_match.py:59, run_eval.py:67, probe.py:68 |
| Hashing | `hash_canonical` / SHA-256 over canonical JSON | canonical_json.py, run_match.py, replay_check.py |
| Docstrings | every module: purpose + PR/Task ref + SDD/NFR citation + "stdlib only (NFR-7)" | all modules |
| Requirement traceability | inline `AC-N`, `FM-N`, `CC-N`, `NFR-N`, `G-N`, `OD-N` tags | pervasive |
| Provenance terms | `regime_id`, `run_id`, `match_id`, `match_index`, `deck_hash`, `trace_hash` | consistent across eval + analysis + schemas |

## Minor divergences (deliberate, flagged not faulted)

### CN-1 Two copies of canonical-hash logic
`analysis/replay_check.py` reimplements the canonical hash instead of importing
`eval/canonical_json.py`, to honor the analysis→analysis-only import rule. Parity is
test-asserted. This is a conscious zone-purity trade, not an inconsistency to "fix".

### CN-2 `evidence_summary.py` local hygiene copy
`analysis/evidence_summary.py:33` keeps a parity-tested local copy of the hygiene path
rules rather than importing `eval/hygiene_check.py` (same zone-purity reason).

## Naming conflicts / breaking-change risks

None found. There are no colliding identifiers, no inconsistent pluralization of the
core nouns, and no competing spellings of `regime`/`run`/`match`. The agent registry
(`eval/run_match.py:AGENTS`) is the single source for agent dispatch and is reused by
`run_eval` and `mirror_validate` — no duplication.

## Improvement opportunities (non-breaking, optional)

- The duplicated canonical-hash (CN-1) could one day move to a shared stdlib-only
  `common/` zone if a third consumer appears — but YAGNI for two call sites with a
  parity test. No action recommended now.
